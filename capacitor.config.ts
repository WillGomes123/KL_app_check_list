
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.econe.checklist',
  appName: 'Checklist KL',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;

